You can form a path (involving at least one slash) to any file in the current working directory by
starting with the dot `.` which is itself a (relative) path referring to the current working
directory.
